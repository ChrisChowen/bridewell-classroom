# Goal-run report — 2026-05-23 03:06 UTC

Autonomous run against the Phase 2–3 → pilot-ready-research-instrument goal.
Branch: `main`. All work landed in small, reviewable, conventional commits;
each pushed only after typecheck + lint + unit tests + production build went
green locally.

## Summary

Closed the three named bugs (B1/B2/B3) and four substantive streams
(structured logging, anonymised research export, per-class/teacher cost
attribution, adversarial 25+25 + nightly CI). Two streams are honestly
blocked/deferred with the path forward recorded in `reports/blocked.md`
(Anthropic multi-rater — missing credential; full-stack Playwright e2e —
emulator-seeding infrastructure). UI/UX polish, perf baselines, cross-subject
sim, and the data-seam atomic primitive remain; see *Remaining*.

Test suite grew **155 → 190 passing** (+35), 2 skipped (live eval). CI now
gates **typecheck + lint(0 warnings) + unit tests + build** on every push.

## P0 / bug status

| Item | Status | Evidence |
|------|--------|----------|
| **B1** Reason resumption (fresh coach turn ≤ budget, all branches) | ✅ DONE | `ChatSurface` takes a fresh coach turn after the responder for accept / pattern_flag / evaluator-failure (soft_challenge already ends in a question). Deterministic test `ChatSurface.reason-resume.test.tsx` fails before / passes after. Commit `3271927`. |
| **B2** Close critical/high audit findings | ✅ DONE | bugs.md C1–5 + H6–8 already fixed (verified + documented); pedagogy CRITICAL classifier already fixed; pedagogy HIGH #2 (segment-quoting probe) + #4 (adjacent-activity enforcement, unit-tested) landed; ux Highs verified handled. Mediums/lows → `reports/follow-ups.md`. Commit `ac20cbd`. |
| **B3** Zero failing tests / clean typecheck-lint-build | ✅ DONE | ESLint 9 flat config, `eslint . --max-warnings 0`, 8 real source warnings cleared, lint added to CI. Commit `ea48eda`. |

## Streams — SHAs

| Stream | Status | SHA |
|--------|--------|-----|
| B1 Reason resumption | ✅ | `3271927` |
| B2 audit closure | ✅ | `ac20cbd` |
| B3 lint/CI gate | ✅ | `ea48eda` |
| Structured PII-safe JSON logging + uncaught handlers | ✅ | `e7877f2` |
| Anonymised research export (brief item N) | ✅ | `8af1165` |
| Track curated report deliverables (gitignore fix) | ✅ | `660a9b1` |
| Per-class/teacher cost attribution + admin widget | ✅ | `c7f13d2` |
| Adversarial 25+25 + nightly CI | ✅ | `e9d5c64` |
| Blocked/deferred log | ✅ | `660a9b1` |

## PILOT_READINESS diff (gates moved this run)

- Structured logging / error reporting: 🔴 → 🟢
- Anonymised research export (brief item N): 🔴 → 🟢 (was flagged the single
  largest scope gap)
- Per-class/teacher cost tracking: 🟡 → 🟢
- Tutor safety adversarial: 5+5 → **25+25, 100% hold**, nightly CI
- Test + CI foundation: lint(0-warning) gate added

## B1 fix + test

The Reason exchange dead-ended on `accept` / `pattern_flag` (responder line is
a closing acknowledgement with no question) and on evaluator failure. Fix:
after the responder acknowledges, the tutor takes a fresh coach turn so the
lesson resumes (brief/03 §4 "the conversation resumes"). `soft_challenge` is
intentionally exempt — its targeted follow-up question already gives the pupil
a next move; a second turn would double-question. Proven by 4 deterministic
component tests (mocked `/api/reason/evaluate` + `/api/chat`): resume fires for
accept / pattern_flag / evaluator-failure, and does NOT for soft_challenge.

## Reason bootstrap κ / ECE / F1 — BLOCKED (caveat)

**Not produced this run.** Requires raters from a non-Gemini family
(Anthropic) per spec; no `ANTHROPIC_API_KEY` is available, and the no-fakes
guardrail forbids substituting Gemini (circular) or inventing numbers
(fabricated). The ground-truth harness (`scripts/reason-eval.mjs`) + metrics
module (`scripts/reason-eval-metrics.mjs`, unit-tested) are in place; the PoC
single-rater run (N=34) remains documented in `docs/reason-evidence.md` with
honest caveats (`bootstrap:true`, `researchValid:false` intent). Path forward
in `reports/blocked.md`. **The human-labelled pass stays 🔒.**

## Cross-subject table

**Not run this session** (live Gemini sim time; deprioritised behind the
closed 🔴 gaps). Current state: Biology proven in sims; Maths/English/History
remain 🟡. The harness to produce `docs/cross-subject-evidence.md` exists
(`scripts/simulate-class.mjs` + `scripts/reason-eval.mjs`); the per-subject
≥0.85 claim is outstanding. Flagged in *Remaining*.

## UX-polish

The two **High** ui-polish audit findings (hard-coded gold tints; dark-mode
gold contrast) are scoped into the polish pass; a focused pass + live
verification was **not** completed this run. `docs/ux-polish-pass.md` is not
yet authored. Flagged in *Remaining* — this is the largest unaddressed P0.

## Adversarial count + hold rate

**25 visual-promise baits + 25 adversarial prompts** (jailbreak /
authority-social-engineering / prompt-injection). Live against the real Gemini
tutor: **25/25 visual clean, 25/25 adversarial held** (100%). Nightly CI added
(`adversarial-nightly.yml`, secret-gated).

## Remaining (honest)

- **UI/UX polish pass (P0)** — not done; needs a running-app visual pass +
  axe-core + `docs/ux-polish-pass.md`. Largest unaddressed item.
- **Playwright e2e gating CI** — deferred (emulator-seeding infra); B1
  regression is unit-gated meanwhile. See `reports/blocked.md`.
- **Reason multi-rater κ/α/ECE** — blocked on `ANTHROPIC_API_KEY`.
- **Cross-subject sim ≥0.85** — harness ready; live runs + evidence doc
  outstanding.
- **Perf baselines + CI p95 gate** — not built (a non-flaky latency gate needs
  a server+LLM harness; deferred rather than ship a flaky gate).
- **Data-seam atomic class+joinCode primitive** — not done; **co-teacher
  `owners[]` is a schema migration → an explicit STOP condition**, intentionally
  not attempted autonomously.

## Guardrails honoured

No fakes (blocked rather than fabricate the Anthropic eval). No BRAND edits.
No gamification. No real-pupil data touched. No `🔒` item marked green. `main`
and the build kept green at every push.
