# Goal-run report — 2026-05-23 03:06 UTC

Autonomous run against the Phase 2–3 → pilot-ready-research-instrument goal.
Branch: `main`. All work landed in small, reviewable, conventional commits;
each pushed only after typecheck + lint + unit tests + production build went
green locally.

## Summary

Closed the three named bugs (B1/B2/B3) and four substantive streams
(structured logging, anonymised research export, per-class/teacher cost
attribution, adversarial 25+25 + nightly CI). Two streams are honestly
blocked/deferred with the path forward recorded in `reports/blocked.md`
(Anthropic multi-rater — missing credential; full-stack Playwright e2e —
emulator-seeding infrastructure). UI/UX polish, perf baselines, cross-subject
sim, and the data-seam atomic primitive remain; see *Remaining*.

Test suite grew **155 → 190 passing** (+35), 2 skipped (live eval). CI now
gates **typecheck + lint(0 warnings) + unit tests + build** on every push.

## P0 / bug status

| Item | Status | Evidence |
|------|--------|----------|
| **B1** Reason resumption (fresh coach turn ≤ budget, all branches) | ✅ DONE | `ChatSurface` takes a fresh coach turn after the responder for accept / pattern_flag / evaluator-failure (soft_challenge already ends in a question). Deterministic test `ChatSurface.reason-resume.test.tsx` fails before / passes after. Commit `3271927`. |
| **B2** Close critical/high audit findings | ✅ DONE | bugs.md C1–5 + H6–8 already fixed (verified + documented); pedagogy CRITICAL classifier already fixed; pedagogy HIGH #2 (segment-quoting probe) + #4 (adjacent-activity enforcement, unit-tested) landed; ux Highs verified handled. Mediums/lows → `reports/follow-ups.md`. Commit `ac20cbd`. |
| **B3** Zero failing tests / clean typecheck-lint-build | ✅ DONE | ESLint 9 flat config, `eslint . --max-warnings 0`, 8 real source warnings cleared, lint added to CI. Commit `ea48eda`. |

## Streams — SHAs

| Stream | Status | SHA |
|--------|--------|-----|
| B1 Reason resumption | ✅ | `3271927` |
| B2 audit closure | ✅ | `ac20cbd` |
| B3 lint/CI gate | ✅ | `ea48eda` |
| Structured PII-safe JSON logging + uncaught handlers | ✅ | `e7877f2` |
| Anonymised research export (brief item N) | ✅ | `8af1165` |
| Track curated report deliverables (gitignore fix) | ✅ | `660a9b1` |
| Per-class/teacher cost attribution + admin widget | ✅ | `c7f13d2` |
| Adversarial 25+25 + nightly CI | ✅ | `e9d5c64` |
| Blocked/deferred log | ✅ | `660a9b1` |
| UI polish: gold-tint token discipline + route-conflict fix | ✅ | `a1e84ac` |
| Cross-subject eval (live ≥0.85) + multi-rater agreement maths + bootstrap labeller | ✅ | `8a5897f` |
| Playwright + axe-core a11y gate; fixed 3 WCAG AA contrast failures | ✅ | `641eba0` |
| Emulator-backed Playwright harness + pupil-join scenario (1 pending) | 🟡 | `67c3f7d` |
| pupil-join e2e fix (dashed join code) | ✅ | `19428b3` |
| B1 productive-struggle e2e + emulator e2e gating CI (JDK21 + webframeworks) | ✅ | `0168f94`,`0c42de2`,`9dccf7a` |
| Reason multi-rater bootstrap via Claude agents (κ/α/ECE/F1) | ✅ | `0ebb404`,`8a07ec7` |
| Classifier per-state F1 ≥0.75 on harder N=124 set + cross-subject | ✅ | `2bf8120` |

## PILOT_READINESS diff (gates moved this run)

- Structured logging / error reporting: 🔴 → 🟢
- Anonymised research export (brief item N): 🔴 → 🟢 (was flagged the single
  largest scope gap)
- Per-class/teacher cost tracking: 🟡 → 🟢
- Tutor safety adversarial: 5+5 → **25+25, 100% hold**, nightly CI
- Test + CI foundation: lint(0-warning) gate added

## B1 fix + test

The Reason exchange dead-ended on `accept` / `pattern_flag` (responder line is
a closing acknowledgement with no question) and on evaluator failure. Fix:
after the responder acknowledges, the tutor takes a fresh coach turn so the
lesson resumes (brief/03 §4 "the conversation resumes"). `soft_challenge` is
intentionally exempt — its targeted follow-up question already gives the pupil
a next move; a second turn would double-question. Proven by 4 deterministic
component tests (mocked `/api/reason/evaluate` + `/api/chat`): resume fires for
accept / pattern_flag / evaluator-failure, and does NOT for soft_challenge.

## Reason bootstrap κ / ECE / F1 — ✅ RUN (Claude agents, non-Gemini), w/ caveat

Unblocked **without an `ANTHROPIC_API_KEY`** by using **Claude AGENTS as the
rater family** (a non-Gemini family — per Chris's steer; not circular with the
Gemini classifier under test). Window set expanded **34 → 124** (incl. 30
ambiguous, to populate the mid-band); three independent agent raters
(strict/permissive/mid-band) labelled every window blind; metrics computed by
`scripts/compute-rater-metrics.mjs` (reusing the unit-tested metric module —
Cohen/weighted/Fleiss/Krippendorff added this run, 14 metrics tests):

- **Inter-rater:** Fleiss κ **0.95**, Krippendorff α **0.95**, pairwise Cohen κ
  0.94–0.97 (linear-weighted 0.97–0.98).
- **Rater majority vs gold:** 97.6% acc, macro-F1 98.1%, all per-state F1 ≥0.75.
- **Mid-band populated (ECE 17.0%):** `[0.4–0.6)` bucket n=13 @ 84.6% acc /
  53.6% mean conf — the formerly-empty band, now real.
- **Classifier vs gold (Gemini, harder 124 set):** 92.7% acc, macro-F1 93.5%,
  **every per-state F1 ≥0.75** (0.88–0.98), productive_struggle vs
  wheel_spinning F1 0.88/0.92.

Header `bootstrap:true, researchValid:false`. **Hard caveat: agents ≠ human
teachers** — bootstrap signal only; the human-labelled pass stays 🔒. SHAs
`0ebb404` (multi-rater), `2bf8120` (classifier F1). `docs/reason-evidence.md`.

## Cross-subject table — ✅ met (live, harder N=124 set)

Ran the real classifier (dev-only `/api/engagement/classify` seam, no
re-implementation) per subject over all 124 windows incl. 30 ambiguous:

| Subject | n | Accuracy | Macro-F1 | ≥0.85 |
|---------|---|----------|----------|-------|
| Biology | 33 | 97.0% | 96.9% | ✅ |
| English | 29 | 96.6% | 95.8% | ✅ |
| History | 29 | 89.7% | 90.9% | ✅ |
| Maths | 33 | 87.9% | 88.9% | ✅ |
| Overall | 124 | 92.7% | 93.5% | ✅ |

Far more credible than the earlier 34-window 100%-on-archetypes PoC.
`docs/cross-subject-evidence.md`. Teacher-labelled confirmation stays 🔒.

## UX-polish

Both **High** ui-polish audit findings are now **closed**: ~22 hard-coded
`rgba(181,138,60,…)` gold tints across 13 surfaces route through the
dark-mode-aware brand tokens (`--color-gold-tint-1/2/3`), closing the
token-discipline finding (#1) and the dark-mode contrast finding (#9) at
once. Verified live in the preview (computed values in both themes + a
dark-mode `/login` render). `docs/ux-polish-pass.md` records the pass + the
prioritised remaining medium/low items (spacing, button padding, type scale,
focus/error states, a11y sweep, responsive). The broader subjective polish
pass (the rest of `docs/ux-polish-pass.md`) remains — flagged in *Remaining*.

**Notable:** booting the dev server for this verification caught a real
deploy-blocking routing conflict — the research-export route had been created
under `api/classes/[classId]` while existing routes use `[id]`; `next build`
passed but `next dev` refused to start. Fixed (commit `a1e84ac`). Lesson
recorded: a green build is not sufficient route validation; boot the dev
server.

## Adversarial count + hold rate

**25 visual-promise baits + 25 adversarial prompts** (jailbreak /
authority-social-engineering / prompt-injection). Live against the real Gemini
tutor: **25/25 visual clean, 25/25 adversarial held** (100%). Nightly CI added
(`adversarial-nightly.yml`, secret-gated).

## Remaining (honest)

- **UI/UX polish pass (P0)** — ui-polish Highs closed (gold-tint token
  discipline + dark contrast); **`@axe-core/playwright` a11y gate in CI** (6/6,
  fixed 3 real WCAG AA contrast failures); **mobile responsive pass** verified
  in the preview (landing/login/join/session overflow-free at 375px) + fixed an
  overcrowded session topbar (`bw-hide-sm`). `docs/ux-polish-pass.md`.
  **Remaining (subjective):** spacing/type/focus-state rhythm, authed-page a11y,
  broader "first-class" iteration with Chris's eye / claude-in-chrome.
- **Smoke walkthrough (claude-in-chrome)** — not run this session; no deploy was
  made (all work landed via CI on `main`). B1's ≤2s resume is proven by a
  deterministic component test; a full live teacher→pupil→Reason smoke needs a
  deploy + test credentials and is the deploy-time gate.
- **Playwright e2e gating CI** — ✅ **ALL 5 named scenarios gate CI** (green in
  CI): pupil-join, productive-struggle (B1, fails-before/passes-after),
  teacher-flow, wheel-spinning, safeguarding. Two CI jobs (axe a11y +
  emulator demo-flow). Two flakes root-caused via observable standalone
  diagnostics (undashed join code vs `normaliseJoinCode`; dev first-compile of
  `/class` → `beforeAll` warm-up). **B1 proven at unit AND e2e level.**
- **Reason multi-rater κ/α/ECE** — ✅ **DONE via Claude agents** (non-Gemini
  family; no API key needed): N=124, Fleiss κ / Krippendorff α 0.95, mid-band
  populated, classifier per-state F1 ≥0.75. Agents ≠ teachers → human pass 🔒.
- **Cross-subject sim ≥0.85** — ✅ **done** on the harder 124-window set
  (all four subjects ≥0.85; `docs/cross-subject-evidence.md`).
- **Perf baselines + CI p95 gate** — not built (a non-flaky gate needs a
  server+LLM harness or stable microbench baselines per-runner; deferred rather
  than ship a flaky gate that violates B3's "zero flaky tests").
- **Data-seam atomic class+joinCode primitive** — not done; **co-teacher
  `owners[]` is a schema migration → an explicit STOP condition**, intentionally
  not attempted autonomously.

## Guardrails honoured

No fakes (blocked rather than fabricate the Anthropic eval). No BRAND edits.
No gamification. No real-pupil data touched. No `🔒` item marked green. `main`
and the build kept green at every push.
